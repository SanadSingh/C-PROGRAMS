# include <iostream>
using namespace std;

int main()
{
    int n=-6;
    if(n)// same as if(n!=0), you can also write if(true) or if(false)
    {
        cout<<"hi";
    }
    else
    {
        cout<<"hello";
    }

    return(0);
}
